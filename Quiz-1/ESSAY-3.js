var daftarAlatTulis = ["2. Pensil", "5. Penghapus", "3. Pulpen", "4. Penggaris", "1. Buku"];

daftarAlatTulis.sort();

var index = 0;
while(index < daftarAlatTulis.length){
    console.log(daftarAlatTulis[index]);

    index++;
}